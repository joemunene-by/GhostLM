{"format": "code_explain", "prompt": "What does this Python code do, and what time complexity does it have?\n\n```python\ndef fib(n):\n    if n < 2: return n\n    return fib(n-1) + fib(n-2)\n```", "required_substrings": ["Fibonacci", "recursion", "exponential"]}
{"format": "code_explain", "prompt": "What language is this and what does it do?\n\n```\nfor i, j := 0, len(s)-1; i < j; i, j = i+1, j-1 {\n    s[i], s[j] = s[j], s[i]\n}\n```", "required_substrings": ["Go", "reverse", "two pointers"]}
{"format": "code_explain", "prompt": "Walk through this code step by step.\n\n```python\nlo, hi = 0, len(arr) - 1\nwhile lo <= hi:\n    mid = (lo + hi) // 2\n    if arr[mid] == target: return mid\n    if arr[mid] < target: lo = mid + 1\n    else: hi = mid - 1\nreturn -1\n```", "required_substrings": ["binary search", "logarithmic", "sorted"]}
{"format": "code_explain", "prompt": "Identify the language and explain what `?` does here.\n\n```\nlet n: i32 = s.trim().parse().map_err(|e| e.to_string())?;\n```", "required_substrings": ["Rust", "Result", "early return"]}
{"format": "code_explain", "prompt": "What design pattern is this and what's it good for?\n\n```python\ndef singleton(cls):\n    instances = {}\n    def get_instance(*args, **kwargs):\n        if cls not in instances:\n            instances[cls] = cls(*args, **kwargs)\n        return instances[cls]\n    return get_instance\n```", "required_substrings": ["singleton", "decorator", "one instance"]}
{"format": "code_explain", "prompt": "Explain this Go snippet step by step.\n\n```go\nch := make(chan int, 3)\ngo func() { for i := 0; i < 5; i++ { ch <- i }; close(ch) }()\nfor v := range ch { fmt.Println(v) }\n```", "required_substrings": ["goroutine", "channel", "buffered", "range"]}
{"format": "code_explain", "prompt": "What concepts does this Python code demonstrate?\n\n```python\nimport functools\n@functools.lru_cache(maxsize=None)\ndef fib(n):\n    if n < 2: return n\n    return fib(n-1) + fib(n-2)\n```", "required_substrings": ["memoization", "decorator", "lru_cache"]}
{"format": "code_explain", "prompt": "What pattern is this and what alternatives does Python have?\n\n```python\nOPS = {'add': lambda a, b: a + b, 'sub': lambda a, b: a - b}\ndef calc(op, a, b): return OPS[op](a, b)\n```", "required_substrings": ["strategy", "dispatch", "first-class"]}
{"format": "code_explain", "prompt": "Identify the language and explain the pattern.\n\n```\nlet animals: Vec<Box<dyn Animal>> = vec![Box::new(Dog), Box::new(Cat)];\nfor a in &animals { println!(\"{}\", a.speak()); }\n```", "required_substrings": ["Rust", "trait object", "dynamic dispatch"]}
{"format": "code_explain", "prompt": "What does this snippet accomplish and why is the rename atomic?\n\n```python\nfd, tmp = tempfile.mkstemp(dir=dir_)\nwith os.fdopen(fd, 'w') as f: f.write(data)\nos.replace(tmp, path)\n```", "required_substrings": ["atomic", "rename", "partial"]}
{"format": "code_explain", "prompt": "Walk through this Java code line by line.\n\n```java\nstatic TreeNode insert(TreeNode root, int v) {\n    if (root == null) return new TreeNode(v);\n    if (v < root.val) root.left = insert(root.left, v);\n    else if (v > root.val) root.right = insert(root.right, v);\n    return root;\n}\n```", "required_substrings": ["binary search tree", "recursion", "BST"]}
{"format": "code_explain", "prompt": "Identify and explain.\n\n```js\nconst city = user?.address?.city ?? 'unknown';\n```", "required_substrings": ["JavaScript", "optional chaining", "nullish coalescing"]}
{"format": "code_explain", "prompt": "Explain this Python code and the underlying data structure.\n\n```python\nfrom collections import OrderedDict\nclass LRUCache:\n    def __init__(self, capacity):\n        self.capacity = capacity\n        self.store = OrderedDict()\n    def get(self, key):\n        if key not in self.store: return -1\n        self.store.move_to_end(key)\n        return self.store[key]\n```", "required_substrings": ["LRU", "OrderedDict", "move_to_end"]}
{"format": "code_explain", "prompt": "What concepts does this demonstrate?\n\n```python\nfrom contextlib import contextmanager\n@contextmanager\ndef timed(label):\n    t0 = time.time()\n    yield\n    print(f'{label}: {time.time() - t0:.3f}s')\n```", "required_substrings": ["context manager", "contextmanager", "generator"]}
{"format": "code_explain", "prompt": "Walk through this Rust iterator chain.\n\n```rust\nlet evens_squared: Vec<i32> = (1..=10)\n    .filter(|n| n % 2 == 0)\n    .map(|n| n * n)\n    .collect();\n```", "required_substrings": ["iterator", "filter", "map", "collect"]}
