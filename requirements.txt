# Convenience shim — pyproject.toml is the single source of truth for
# dependencies. This installs the package editable with every extra
# (training, export, data collection, serving, dev tooling).
-e .[train,export,data,serve,dev]
